# i18n Rules

The Next.js app and docs app share locale data from `libs/i18n`.

## Rules

- Add keys to `libs/i18n/locales/en.ts`.
- Mirror key shape in `libs/i18n/locales/zh-CN.ts`.
- Do not hardcode user-facing strings in pages/components.
- Use the existing Next translation helpers/hooks.
