# Validator Rules

Use shared Zod validators from `libs/validators`.

## Rules

- Prefer shared schemas over ad hoc validation.
- Keep schemas framework-neutral.
- Surface errors through i18n-aware UI where possible.
